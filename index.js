// Write your code in this file!
const currentUser = 'Grace Hopper'; //declaring the currentuser variable

const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`; //declaring the welcomeMessage variable

const excitedWelcomeMessage = welcomeMessage.toUpperCase(); //declaring the excitedWelcomeMessage variable

const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`; //declaring a new variable and slicing to only return its first char

